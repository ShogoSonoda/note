<div class="container">
  <div class="row d-flex flex-column m-auto" style="width:610px">
    <% @posts.each do |post| %>
      <%= render partial: 'post', locals: { post: post } %>
    <% end %>
  </div>
</div>